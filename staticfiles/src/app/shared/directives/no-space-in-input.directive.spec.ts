import { NoSpaceInInputDirective } from './no-space-in-input.directive';

describe('NoSpaceInInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpaceInInputDirective();
    expect(directive).toBeTruthy();
  });
});
